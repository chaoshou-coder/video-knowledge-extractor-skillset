[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "video-knowledge-extractor-skillset"
version = "0.1.0"
description = "Skills-first runtime for transcript-to-knowledge extraction"
requires-python = ">=3.10"
dependencies = [
    "click>=8.1.0",
    "httpx>=0.26.0",
    "pydantic>=2.0.0",
    "python-frontmatter>=1.0.0",
    "tomli>=2.0.0; python_version < '3.11'",
]

[project.optional-dependencies]
vector = ["chromadb>=0.4.22", "sentence-transformers>=2.2.0"]
export = ["ebooklib>=0.18", "markdown>=3.5.0"]
full = ["chromadb", "sentence-transformers", "ebooklib", "markdown", "weasyprint>=60.0"]

[project.scripts]
kl = "src.cli:main"
